import sidebarLinks from '../../../utils/sidebar-links';
import { module, test } from 'qunit';

module('Unit | Utility | sidebar links');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sidebarLinks();
  assert.ok(result);
});
