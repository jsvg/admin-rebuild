import macrosGroupBy from '../../../utils/macros/group-by';
import { module, test } from 'qunit';

module('Unit | Utility | macros/group by');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = macrosGroupBy();
  assert.ok(result);
});
