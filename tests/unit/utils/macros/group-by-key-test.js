import macrosGroupByKey from '../../../utils/macros/group-by-key';
import { module, test } from 'qunit';

module('Unit | Utility | macros/group by key');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = macrosGroupByKey();
  assert.ok(result);
});
