import macrosAvgGroup from '../../../utils/macros/avg-group';
import { module, test } from 'qunit';

module('Unit | Utility | macros/avg group');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = macrosAvgGroup();
  assert.ok(result);
});
