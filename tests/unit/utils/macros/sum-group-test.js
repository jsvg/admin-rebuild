import macrosSumGroup from '../../../utils/macros/sum-group';
import { module, test } from 'qunit';

module('Unit | Utility | macros/sum group');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = macrosSumGroup();
  assert.ok(result);
});
