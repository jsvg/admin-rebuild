import macrosLenGroup from '../../../utils/macros/len-group';
import { module, test } from 'qunit';

module('Unit | Utility | macros/len group');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = macrosLenGroup();
  assert.ok(result);
});
